<div id="mood_submitted" class="grid-container m-0">
  <% { "Casual" => "😊", "Classy" => "✨", "Dancing" => "🪩", "Music" => "🎶", "Unique" => "🎭", "Anything" => "🙌" }.each do |mood, emoji| %>

    <% if plan.polls.where(submitted: true, mood: mood).exists? %>
      <div>
        <div>
          <p style="font-size: 32px;" class="poll-emoji grid-1"><%= emoji %></p>
        </div>
        <div>
          <p style="font-size: 10px; display: flex; justify-content: center; margin-top: 4px;"><%= mood.capitalize %></p>
        </div>
      </div>
      <div>
        <% plan.polls.where(submitted: true, mood: mood).each do |poll| %>
          <%= render "polls/subpoll", poll: poll %>
        <% end %>
      </div>
    <% end %>
  <% end %>
</div>

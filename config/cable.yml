development:
  adapter: async
  adapter: enhanced_postgresql

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: plans_tonight_production
  adapter: enhanced_postgresql
